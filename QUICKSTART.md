# 🚀 Быстрый старт AZR Model Trainer

## Шаг 1: Установка зависимостей

Откройте командную строку в папке проекта и выполните:

```bash
python -m pip install torch fastapi uvicorn python-multipart pydantic numpy
```

Или используйте:
```bash
python -m pip install -r requirements.txt
```

## Шаг 2: Проверка установки (опционально)

Запустите тест:
```bash
python test_model.py
```

Это проверит, что все компоненты работают корректно.

## Шаг 3: Запуск сервера

### Вариант 1: Через start.bat
Дважды кликните на файл `start.bat`

### Вариант 2: Через командную строку
```bash
python server.py
```

Сервер запустится на http://localhost:8000

## Шаг 4: Откройте веб-интерфейс

Откройте браузер и перейдите по адресу:
```
http://localhost:8000
```

## Шаг 5: Создайте первую модель

1. На вкладке "Создать модель":
   - Название: `my_first_ai`
   - Оставьте параметры по умолчанию
   - Нажмите "Создать модель"

2. Дождитесь сообщения об успехе ✅

## Шаг 6: Обучите модель

1. Перейдите на вкладку "Обучение"

2. Загрузите книгу:
   - Используйте example_book.txt из папки books
   - Или загрузите свой .txt файл

3. Настройте обучение:
   - Выберите модель: `my_first_ai`
   - Выберите книгу
   - Max Iterations: 10000 (для быстрого теста)
   - Нажмите "Начать обучение"

4. Следите за прогрессом в реальном времени

## Шаг 7: Генерируйте текст

1. Перейдите на вкладку "Генерация"

2. Выберите обученную модель

3. Введите промпт, например:
   ```
   Искусственный интеллект
   ```

4. Нажмите "Сгенерировать"

5. Наслаждайтесь результатом! 🎉

## Шаг 8: Скачайте модель

1. Перейдите на вкладку "Мои модели"
2. Нажмите "Скачать модель"
3. Модель сохранится как .pt файл

---

## 🎯 Совет для первого эксперимента

Создайте **маленькую модель** для быстрого теста:
- d_model: 128
- num_layers: 4
- num_heads: 4
- max_iterations: 10000

Это позволит увидеть результаты через 10-15 минут!

Для **серьёзного обучения** увеличьте:
- d_model: 512
- num_layers: 12
- max_iterations: 1000000

---

## ❓ Проблемы?

### "ModuleNotFoundError: No module named 'torch'"
```bash
python -m pip install torch
```

### "ModuleNotFoundError: No module named 'fastapi'"
```bash
python -m pip install fastapi uvicorn python-multipart
```

### Сервер не запускается
1. Проверьте, что порт 8000 свободен
2. Закройте другие программы, использующие порт
3. Попробуйте изменить порт в server.py (последняя строка)

### Медленное обучение
- Уменьшите размер модели (d_model, num_layers)
- Уменьшите batch_size
- Если есть NVIDIA GPU, установите PyTorch с CUDA

---

## 🎨 Эксперименты

Попробуйте обучить модели на разных текстах:
- 📚 Классическая литература
- 🔬 Научные статьи
- 💬 Диалоги и сценарии
- 📰 Новости
- 🎭 Поэзия

Каждый стиль даст уникальные результаты!

---

**Готово! Начинайте создавать AI! 🧠✨**
