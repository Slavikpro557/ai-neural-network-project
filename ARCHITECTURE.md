# 🏗️ Архитектура AZR Model Trainer

## Обзор

AZR Model Trainer - это система самообучающихся языковых моделей, основанная на методе **Absolute Zero Reasoner**. Ключевая идея: модель учится улучшать себя через **self-play** без необходимости в постоянном внешнем надзоре.

---

## 📊 Компоненты системы

### 1. Трансформерная архитектура (`model.py`)

#### CustomTransformerLM
Кастомная реализация языковой модели на основе трансформера:

```
Входной текст
    ↓
Token Embedding + Position Embedding
    ↓
Transformer Blocks (N слоёв)
    ├─ Multi-Head Self-Attention
    ├─ LayerNorm
    ├─ Feed-Forward Network
    └─ LayerNorm
    ↓
Linear Head (vocab_size)
    ↓
Выходные вероятности токенов
```

**Параметры:**
- `vocab_size`: Размер словаря (количество уникальных токенов)
- `d_model`: Размерность внутреннего представления (128-512)
- `num_layers`: Количество трансформер-блоков (4-12)
- `num_heads`: Количество голов внимания (4-16)
- `d_ff`: Размерность feed-forward сети (обычно 4×d_model)
- `max_seq_len`: Максимальная длина последовательности

#### Multi-Head Attention
Механизм внимания позволяет модели фокусироваться на релевантных частях входа:

```python
Q, K, V = Linear(x)  # Query, Key, Value
Attention = softmax(QK^T / √d_k) × V
```

Каждая "голова" учит свой аспект внимания (синтаксис, семантика, etc.)

---

### 2. Токенизатор (`tokenizer.py`)

#### SimpleTokenizer
Преобразует текст в числовые токены и обратно:

**Процесс:**
1. **Обучение**: Анализирует корпус текста, выделяет топ-N токенов
2. **Кодирование**: Текст → Список ID токенов
3. **Декодирование**: ID → Текст

**Специальные токены:**
- `<PAD>` (0): Паддинг для выравнивания длины
- `<UNK>` (1): Неизвестный токен
- `<BOS>` (2): Начало последовательности
- `<EOS>` (3): Конец последовательности

---

### 3. AZR Trainer (`azr_trainer.py`)

#### Метод Absolute Zero Reasoner

**Цикл обучения:**

```
1. SELF-PLAY ФАЗА
   ├─ Модель генерирует текст из промптов
   ├─ Оценивается качество генерации
   └─ Вычисляется награда (reward)

2. ОБУЧЕНИЕ
   ├─ Батч из реальных данных
   ├─ Forward pass
   ├─ Вычисление loss
   ├─ Backward pass
   └─ Обновление весов

3. ПОВТОР → Бесконечное улучшение
```

#### Функция награды (Reward)

Текущая реализация оценивает:

```python
reward = diversity_score × 0.5 + length_score × 0.5
```

- **Diversity**: Уникальность токенов (предотвращает повторения)
- **Length**: Длина генерации (поощряет полные мысли)

**Можно расширить:**
- Когерентность (связность текста)
- Грамматическая корректность
- Соответствие промпту
- Новизна идей

---

### 4. FastAPI Backend (`server.py`)

#### REST API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Веб-интерфейс |
| `/create_model` | POST | Создание новой модели |
| `/upload_book` | POST | Загрузка текста для обучения |
| `/books` | GET | Список загруженных книг |
| `/models` | GET | Список созданных моделей |
| `/train` | POST | Запуск обучения (фоновый процесс) |
| `/training_status` | GET | Статус текущего обучения |
| `/generate` | POST | Генерация текста |
| `/download_model/{name}` | GET | Скачать обученную модель |

#### Асинхронное обучение

Обучение запускается в отдельном потоке, чтобы не блокировать сервер:

```python
thread = threading.Thread(target=train_model_background)
thread.start()
```

Статус доступен через polling endpoint `/training_status`

---

## 🔄 Процесс обучения

### Этап 1: Подготовка данных

```
Книга (текст)
    ↓
Разбиение на чанки (overlapping)
    ↓
Токенизация
    ↓
Создание пар (input, target)
    ↓
DataLoader (батчи)
```

### Этап 2: Основной цикл

```python
for epoch in range(max_epochs):
    for batch in dataloader:
        # Supervised Learning
        loss = model(batch.input, batch.target)
        loss.backward()
        optimizer.step()
        
        # Self-Play (периодически)
        if iteration % 10 == 0:
            generated = model.generate(prompts)
            rewards = compute_reward(generated)
            # Используется для оценки прогресса
```

### Этап 3: Оптимизация

- **Optimizer**: AdamW с weight decay
- **Scheduler**: Cosine Annealing (плавное снижение LR)
- **Gradient Clipping**: Предотвращает взрывающиеся градиенты

```python
optimizer = AdamW(model.parameters(), lr=3e-4, weight_decay=0.01)
scheduler = CosineAnnealingLR(optimizer, T_max=max_iterations)
clip_grad_norm_(model.parameters(), max_norm=1.0)
```

---

## 🧮 Математика трансформера

### Self-Attention

```
Attention(Q, K, V) = softmax(QK^T / √d_k) × V

где:
Q = xW_Q  (queries)
K = xW_K  (keys)
V = xW_V  (values)
```

### Multi-Head Attention

```
MultiHead(x) = Concat(head_1, ..., head_h) × W_O

где head_i = Attention(xW_Q^i, xW_K^i, xW_V^i)
```

### Feed-Forward Network

```
FFN(x) = GELU(xW_1 + b_1) × W_2 + b_2
```

### Layer Normalization

```
LayerNorm(x) = γ × (x - μ) / √(σ² + ε) + β
```

---

## 🎯 Генерация текста

### Авторегрессивная генерация

```python
for _ in range(max_new_tokens):
    logits = model(context)
    next_token = sample(logits[-1])
    context = append(context, next_token)
```

### Стратегии сэмплирования

1. **Greedy**: Выбор токена с максимальной вероятностью
   ```python
   next_token = argmax(probs)
   ```

2. **Temperature Sampling**: Контроль случайности
   ```python
   probs = softmax(logits / temperature)
   next_token = multinomial(probs)
   ```
   - T → 0: детерминированно
   - T = 1: нормальное распределение
   - T → ∞: случайно

3. **Top-K Sampling**: Ограничение выбора
   ```python
   top_k_probs = probs[topk(probs, k)]
   next_token = multinomial(top_k_probs)
   ```

---

## 📈 Метрики качества

### Loss (Функция потерь)

```
CrossEntropyLoss = -Σ y_true × log(y_pred)
```

Меньше → Лучше (модель точнее предсказывает)

### Perplexity

```
Perplexity = exp(CrossEntropyLoss)
```

Интуитивная метрика "удивления" модели

### Reward (Награда в self-play)

Кастомная метрика качества генерации
- Больше → Лучше

---

## 🔧 Настройка гиперпараметров

### Размер модели

| Параметр | Малая | Средняя | Большая |
|----------|-------|---------|---------|
| d_model | 128 | 256-384 | 512-768 |
| num_layers | 4-6 | 8-10 | 12-16 |
| num_heads | 4 | 8 | 12-16 |
| **Параметры** | ~5M | ~20M | ~100M+ |

### Обучение

- **Learning Rate**: 1e-4 до 5e-4 (меньше для больших моделей)
- **Batch Size**: 8-32 (зависит от памяти GPU)
- **Max Iterations**: 10K (тест) до 1M+ (продакшн)
- **Save Every**: 500-1000 итераций

### Генерация

- **Temperature**: 0.5 (консервативно) до 1.5 (креативно)
- **Top-K**: 20-50 (баланс качества и разнообразия)

---

## 💡 Преимущества AZR подхода

1. **Самообучение**: Модель улучшается без дополнительных данных
2. **Адаптивность**: Self-play позволяет находить слабые места
3. **Масштабируемость**: Работает от малых до больших моделей
4. **Гибкость**: Легко изменить функцию награды под задачу

---

## 🚀 Будущие улучшения

- [ ] Reinforcement Learning (PPO, A3C) для оптимизации
- [ ] Beam Search для детерминированной генерации
- [ ] Nucleus (Top-P) sampling
- [ ] Multi-task learning (перевод, суммаризация)
- [ ] Model distillation (сжатие модели)
- [ ] Distributed training (обучение на нескольких GPU)
- [ ] LoRA/QLoRA для эффективного fine-tuning
- [ ] Evaluation suite (BLEU, ROUGE, BERTScore)

---

## 📚 Литература и источники

1. **Attention Is All You Need** (Vaswani et al., 2017)
   - Оригинальная статья про трансформеры

2. **Language Models are Unsupervised Multitask Learners** (Radford et al., 2019)
   - GPT-2, основы авторегрессивного моделирования

3. **Training language models to follow instructions with human feedback** (Ouyang et al., 2022)
   - RLHF, InstructGPT

4. **Absolute Zero Reasoner** (2025)
   - Self-play для языковых моделей

---

**Создано с ❤️ для исследователей и энтузиастов AI**
