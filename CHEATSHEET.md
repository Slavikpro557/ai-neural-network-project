# 📋 Шпаргалка AZR Model Trainer

## ⚡ Быстрые команды

### Установка
```bash
pip install -r requirements.txt
```

### Запуск сервера
```bash
python server.py
# Откройте http://localhost:8000
```

### Тестирование
```bash
python test_model.py
```

### CLI демо
```bash
python cli_demo.py
```

---

## 🔧 Параметры модели

| Параметр | Малая | Средняя | Большая |
|----------|-------|---------|---------|
| **vocab_size** | 5000 | 8000 | 15000 |
| **d_model** | 128 | 256-384 | 512-768 |
| **num_layers** | 4 | 6-8 | 10-16 |
| **num_heads** | 4 | 8 | 12-16 |
| **d_ff** | 512 | 1024-1536 | 2048-3072 |
| **max_seq_len** | 128 | 256 | 512-1024 |
| **Параметры** | ~1-5M | ~10-30M | ~50-200M |
| **Время (10K iter)** | 5-10 мин | 20-40 мин | 1-3 часа |

---

## 📊 Гиперпараметры обучения

### Быстрый тест
```
Max Iterations: 1,000
Batch Size: 8
Learning Rate: 0.001
```

### Стандартное обучение
```
Max Iterations: 50,000
Batch Size: 16
Learning Rate: 0.0003
Save Every: 1,000
```

### Продакшн
```
Max Iterations: 500,000+
Batch Size: 32
Learning Rate: 0.0001
Save Every: 5,000
```

---

## 🎯 Параметры генерации

### Точная генерация
```json
{
  "temperature": 0.5,
  "top_k": 20,
  "max_length": 50
}
```

### Сбалансированная
```json
{
  "temperature": 0.8,
  "top_k": 40,
  "max_length": 100
}
```

### Креативная
```json
{
  "temperature": 1.2,
  "top_k": 50,
  "max_length": 200
}
```

---

## 📁 Структура файлов

```
для ии/
├── model.py              # Архитектура
├── tokenizer.py          # Токенизация
├── azr_trainer.py        # Обучение
├── server.py             # API сервер
├── templates/index.html  # UI
├── models/               # Ваши модели
├── books/                # Ваши тексты
└── checkpoints/          # Чекпоинты
```

---

## 🐍 Python API

### Создать модель
```python
from model import CustomTransformerLM

model = CustomTransformerLM(
    vocab_size=8000,
    d_model=256,
    num_layers=6,
    num_heads=8,
    d_ff=1024,
    max_seq_len=256
)
```

### Создать токенизатор
```python
from tokenizer import SimpleTokenizer

tokenizer = SimpleTokenizer(vocab_size=8000)
tokenizer.train(texts)
tokenizer.save("tokenizer.pkl")
```

### Обучить
```python
from azr_trainer import AZRTrainer

trainer = AZRTrainer(model, tokenizer)
trainer.train_continuous(
    texts=texts,
    max_iterations=10000,
    batch_size=16,
    lr=3e-4
)
```

### Сгенерировать
```python
import torch

prompt = "Искусственный интеллект"
tokens = tokenizer.encode(prompt)
idx = torch.tensor([tokens])

generated = model.generate(
    idx, 
    max_new_tokens=50,
    temperature=0.8,
    top_k=40
)

text = tokenizer.decode(generated[0].tolist())
print(text)
```

---

## 🌐 REST API

### Создать модель
```bash
curl -X POST http://localhost:8000/create_model \
  -H "Content-Type: application/json" \
  -d '{
    "name": "my_model",
    "vocab_size": 8000,
    "d_model": 256,
    "num_layers": 6,
    "num_heads": 8,
    "d_ff": 1024,
    "max_seq_len": 256
  }'
```

### Начать обучение
```bash
curl -X POST http://localhost:8000/train \
  -H "Content-Type: application/json" \
  -d '{
    "model_name": "my_model",
    "book_file": "book.txt",
    "max_iterations": 10000,
    "batch_size": 16,
    "learning_rate": 0.0003,
    "save_every": 1000
  }'
```

### Статус обучения
```bash
curl http://localhost:8000/training_status
```

### Генерация
```bash
curl -X POST http://localhost:8000/generate \
  -H "Content-Type: application/json" \
  -d '{
    "model_name": "my_model",
    "prompt": "Искусственный интеллект",
    "max_length": 100,
    "temperature": 0.8,
    "top_k": 40
  }'
```

---

## 🔍 Диагностика проблем

### Loss не уменьшается
- ✅ Уменьшите learning rate (0.0001)
- ✅ Увеличьте размер модели
- ✅ Проверьте качество данных
- ✅ Увеличьте batch size

### Генерация повторяется
- ✅ Увеличьте temperature (>1.0)
- ✅ Увеличьте top_k (50-100)
- ✅ Больше обучения
- ✅ Разнообразнее данные

### Out of memory
- ✅ Уменьшите batch_size (8 → 4 → 2)
- ✅ Уменьшите max_seq_len
- ✅ Уменьшите d_model
- ✅ Уменьшите num_layers

### Медленное обучение (CPU)
- ✅ Установите PyTorch с CUDA
- ✅ Уменьшите размер модели
- ✅ Уменьшите max_iterations для теста

---

## 💡 Советы и трюки

### Быстрое прототипирование
```python
# Минимальная модель для тестов
d_model=64, num_layers=2, max_iterations=500
```

### Лучшее качество
```python
# Больше данных + больше итераций
max_iterations=100000+, diverse_data=True
```

### Специализация
```python
# Узкая тема + много итераций
specific_domain=True, max_iterations=50000
```

### Transfer Learning
```python
# Сначала общий корпус, потом специализация
1. Train on general text (50K)
2. Fine-tune on specific (10K, lr=1e-4)
```

---

## 🎨 Примеры промптов

### Для классики
```
"В светском обществе"
"Однажды в холодный зимний вечер"
"Князь задумался о"
```

### Для sci-fi
```
"Космический корабль приближался к"
"В далёком будущем человечество"
"Искусственный интеллект осознал"
```

### Для науки
```
"Нейронные сети представляют собой"
"Квантовая механика описывает"
"Эксперимент показал, что"
```

### Для кода
```
"def calculate_"
"class DataProcessor:"
"import numpy as np"
```

---

## 📈 Целевые метрики

### После обучения
| Итераций | Loss | Quality | Use Case |
|----------|------|---------|----------|
| 1K | 5-6 | Очень низкое | Тест системы |
| 10K | 3-4 | Низкое | Демо |
| 50K | 2-3 | Среднее | Эксперименты |
| 100K | 1.5-2.5 | Хорошее | Прототипы |
| 500K+ | 1-1.5 | Отличное | Продакшн |

---

## 🚀 Workflow

### Типичный процесс

1. **Создать модель** (30 сек)
2. **Загрузить данные** (1 мин)
3. **Начать обучение** (click)
4. **☕ Перерыв** (10 мин - несколько часов)
5. **Проверить генерацию** (1 мин)
6. **Если нужно → продолжить обучение**
7. **Скачать модель**
8. **Profit! 🎉**

---

## 🔥 Горячие клавиши

В веб-интерфейсе (планируется):
- `Ctrl+N` - Новая модель
- `Ctrl+U` - Загрузить книгу
- `Ctrl+T` - Начать обучение
- `Ctrl+G` - Генерация
- `Ctrl+S` - Сохранить модель

---

## 📞 Быстрая помощь

### Где искать
1. `README.md` - Основное руководство
2. `QUICKSTART.md` - Быстрый старт
3. `ARCHITECTURE.md` - Как работает
4. `EXAMPLES.md` - Примеры
5. `FEATURES.md` - Все возможности

### Тестовые команды
```bash
# Проверить Python
python --version

# Проверить PyTorch
python -c "import torch; print(torch.__version__)"

# Проверить CUDA
python -c "import torch; print(torch.cuda.is_available())"

# Тест системы
python test_model.py

# CLI демо
python cli_demo.py
```

---

## 💾 Сохранение и загрузка

### Сохранить модель
```python
torch.save(model.state_dict(), "model.pt")
tokenizer.save("tokenizer.pkl")
```

### Загрузить модель
```python
model.load_state_dict(torch.load("model.pt"))
tokenizer = SimpleTokenizer.load("tokenizer.pkl")
```

### Через веб-интерфейс
- Вкладка "Мои модели"
- Кнопка "Скачать модель"

---

## 🎯 Чеклист перед продакшн

- [ ] Обучено >100K итераций
- [ ] Loss <2.0
- [ ] Протестирована генерация
- [ ] Сохранены чекпоинты
- [ ] Документирован процесс
- [ ] Настроены параметры генерации
- [ ] Проверена на edge cases

---

**Успешного обучения! 🧠✨**

*Распечатайте эту шпаргалку и держите рядом!*
