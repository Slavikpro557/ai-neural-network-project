# 📚 Руководство по работе с множественными датасетами

## 🎯 Что нового:

### ✅ Возможности:
1. **Прикрепление нескольких датасетов** к одной модели
2. **Автоматическая загрузка** всех прикреплённых при обучении
3. **Открепление датасетов** - они возвращаются в общий список
4. **Сохранение связей** - прикреплённые датасеты запоминаются
5. **Управление через API** и веб-интерфейс

---

## 🚀 Быстрый старт:

### 1. Запустите обновлённый сервер:

```bash
python server_with_datasets.py
```

### 2. Загрузите несколько книг:

```python
# Через API или веб-интерфейс
POST /upload_book
files: book1.txt, book2.txt, book3.txt
```

### 3. Прикрепите к модели:

```python
POST /attach_dataset
{
    "model_name": "my_model",
    "dataset_name": "book1.txt"
}

POST /attach_dataset
{
    "model_name": "my_model",
    "dataset_name": "book2.txt"
}
```

### 4. Запустите обучение:

```python
POST /train
{
    "model_name": "my_model",
    "max_iterations": 10000
}
# Автоматически загрузит book1.txt И book2.txt!
```

---

## 📊 Архитектура системы:

```
┌─────────────────────────────────────┐
│      Dataset Manager (JSON DB)      │
│                                     │
│  models: {                          │
│    "my_model": {                    │
│      "datasets": ["book1", "book2"] │
│    }                                │
│  }                                  │
│                                     │
│  datasets: {                        │
│    "book1": {                       │
│      "path": "books/book1.txt",     │
│      "attached_to": ["my_model"]    │
│    }                                │
│  }                                  │
└─────────────────────────────────────┘
```

### Workflow:

```
1. Загрузка файла
   ├─> Сохранение в books/
   └─> Регистрация в dataset_manager

2. Прикрепление к модели
   ├─> models["my_model"].datasets.add("book1")
   └─> datasets["book1"].attached_to.add("my_model")

3. Обучение модели
   ├─> Загрузить все прикреплённые датасеты
   ├─> Объединить в один список текстов
   └─> Обучить модель

4. Открепление
   ├─> Удалить из модели
   └─> Датасет остаётся доступным для других моделей
```

---

## 🔧 API Reference:

### POST /attach_dataset
Прикрепить датасет к модели

**Request:**
```json
{
    "model_name": "my_model",
    "dataset_name": "book1.txt"
}
```

**Response:**
```json
{
    "status": "success",
    "message": "Dataset 'book1.txt' attached to 'my_model'"
}
```

---

### POST /detach_dataset
Открепить датасет от модели

**Request:**
```json
{
    "model_name": "my_model",
    "dataset_name": "book1.txt"
}
```

**Response:**
```json
{
    "status": "success",
    "message": "Dataset 'book1.txt' detached from 'my_model'"
}
```

---

### GET /model_datasets/{model_name}
Получить датасеты модели

**Response:**
```json
{
    "attached": [
        {
            "name": "book1.txt",
            "size": 50000,
            "path": "books/book1.txt"
        }
    ],
    "available": [
        {
            "name": "book2.txt",
            "size": 75000,
            "path": "books/book2.txt",
            "attached_to": []
        }
    ]
}
```

---

### GET /dataset_stats
Общая статистика

**Response:**
```json
{
    "total_models": 3,
    "total_datasets": 10,
    "attachments": 15
}
```

---

## 💡 Примеры использования:

### Сценарий 1: Специализированная модель

```python
# Создаём модель для технической документации
create_model("tech_docs_model")

# Прикрепляем только технические тексты
attach("tech_docs_model", "python_docs.txt")
attach("tech_docs_model", "javascript_docs.txt")
attach("tech_docs_model", "api_reference.txt")

# Обучение
train("tech_docs_model", max_iterations=50000)

# Результат: Модель специализируется на техническом тексте
```

---

### Сценарий 2: Универсальная модель

```python
# Создаём универсальную модель
create_model("universal_model")

# Прикрепляем разнообразные тексты
attach("universal_model", "classic_literature.txt")
attach("universal_model", "modern_news.txt")
attach("universal_model", "code_samples.txt")
attach("universal_model", "scientific_papers.txt")
attach("universal_model", "dialogues.txt")

# Обучение на всём сразу
train("universal_model", max_iterations=100000)

# Результат: Модель работает в разных доменах
```

---

### Сценарий 3: Постепенное добавление

```python
# День 1: Классика
create_model("progressive_model")
attach("progressive_model", "tolstoy.txt")
train("progressive_model", max_iterations=10000)

# День 2: Добавляем современное
attach("progressive_model", "modern_literature.txt")
train("progressive_model", max_iterations=20000, resume_from="...")

# День 3: Добавляем техническое
attach("progressive_model", "technical.txt")
train("progressive_model", max_iterations=30000, resume_from="...")

# Результат: Модель учит разное постепенно
```

---

### Сценарий 4: A/B тестирование

```python
# Модель A: Только классика
create_model("model_a")
attach("model_a", "classic1.txt")
attach("model_a", "classic2.txt")
train("model_a", max_iterations=10000)

# Модель B: Классика + современное
create_model("model_b")
attach("model_b", "classic1.txt")
attach("model_b", "classic2.txt")
attach("model_b", "modern1.txt")
attach("model_b", "modern2.txt")
train("model_b", max_iterations=10000)

# Сравнение качества генерации
```

---

### Сценарий 5: Динамическое управление

```python
# Начальная конфигурация
create_model("dynamic_model")
attach("dynamic_model", "dataset1.txt")
attach("dynamic_model", "dataset2.txt")
train(max_iterations=5000)

# Передумали: dataset2 не нужен
detach("dynamic_model", "dataset2.txt")
attach("dynamic_model", "dataset3.txt")  # Вместо него

# Продолжаем обучение с новой конфигурацией
train(max_iterations=10000, resume_from="...")
```

---

## 🎨 Преимущества:

### ✅ Гибкость:
```
Можно создать разные модели с разными комбинациями датасетов:
- model_classic: book1 + book2
- model_modern: book3 + book4
- model_mixed: book1 + book3 + book5
```

### ✅ Переиспользование:
```
Один датасет может быть прикреплён к нескольким моделям:
- book1.txt → [model_a, model_b, model_c]
```

### ✅ Управляемость:
```
Видно какие данные использует каждая модель:
- my_model → [dataset1, dataset2, dataset3] (150 KB total)
```

### ✅ Экономия времени:
```
Не нужно каждый раз выбирать файлы:
- Прикрепили раз → используются автоматически
```

---

## 🔍 Отладка:

### Проверить прикреплённые датасеты:

```python
from dataset_manager import DatasetManager

manager = DatasetManager()

# Список прикреплённых
attached = manager.get_attached_datasets("my_model")
print(f"Attached: {attached}")

# Информация о модели
info = manager.get_model_info("my_model")
print(f"Total size: {info['total_size']} bytes")
print(f"Datasets: {info['total_datasets']}")

# Пути к файлам
paths = manager.get_dataset_paths("my_model")
for path in paths:
    print(f"  - {path}")
```

### Просмотр базы данных:

```bash
cat datasets_db.json
```

```json
{
  "models": {
    "my_model": {
      "datasets": ["book1.txt", "book2.txt"],
      "created": "2025-10-20T19:30:00"
    }
  },
  "datasets": {
    "book1.txt": {
      "path": "books/book1.txt",
      "size": 50000,
      "attached_to": ["my_model"],
      "registered": "2025-10-20T19:25:00"
    }
  }
}
```

---

## ⚠️ Важные замечания:

### 1. Порядок прикрепления не важен:
```python
attach("model", "book1")
attach("model", "book2")
# То же что:
attach("model", "book2")
attach("model", "book1")
```

### 2. Дубликаты игнорируются:
```python
attach("model", "book1")
attach("model", "book1")  # Игнорируется
# Результат: ["book1"] (не ["book1", "book1"])
```

### 3. Открепление не удаляет файл:
```python
detach("model", "book1")
# Файл books/book1.txt остаётся
# Можно прикрепить к другой модели
```

### 4. Удаление датасета:
```python
manager.delete_dataset("book1")
# Открепляет от всех моделей
# Удаляет из БД
# НО НЕ удаляет файл books/book1.txt
```

---

## 🎯 Best Practices:

### 1. Именование датасетов:
```
✅ classic_tolstoy.txt
✅ modern_news_2024.txt
✅ python_docs_3.12.txt

❌ book.txt
❌ file1.txt
❌ data.txt
```

### 2. Группировка:
```python
# Создайте отдельные модели для разных целей
model_literature = ["classic1", "classic2", "modern1"]
model_technical = ["docs1", "docs2", "code1"]
model_mixed = ["classic1", "docs1", "news1"]
```

### 3. Валидация:
```python
# Перед обучением проверьте
attached = manager.get_attached_datasets("my_model")
if len(attached) == 0:
    print("ERROR: No datasets!")
elif len(attached) < 3:
    print("WARNING: Only", len(attached), "datasets")
```

### 4. Резервное копирование:
```bash
# Регулярно сохраняйте БД
cp datasets_db.json datasets_db.backup.json
```

---

## 📊 Рекомендуемые конфигурации:

| Задача | Датасетов | Размер каждого | Итераций |
|--------|-----------|----------------|----------|
| Имитация автора | 1-2 | 500 KB | 10,000 |
| Специализация | 3-5 | 1 MB | 50,000 |
| Универсальная | 10-20 | 500 KB | 100,000 |
| GPT-like | 100+ | 1-10 MB | 500,000+ |

---

## 🚀 Миграция с одиночных файлов:

### Старый способ:
```python
train(book_file="book1.txt", ...)
# Каждый раз выбирать вручную
```

### Новый способ:
```python
# Один раз прикрепить
attach("my_model", "book1.txt")
attach("my_model", "book2.txt")

# Потом просто
train(model_name="my_model", ...)
# Автоматически использует все прикреплённые
```

---

## 📝 Итого:

### Теперь вы можете:
1. ✅ Загружать много датасетов
2. ✅ Прикреплять их к модели
3. ✅ Обучать на всех сразу
4. ✅ Открепять ненужные
5. ✅ Добавлять новые по ходу
6. ✅ Не выбирать каждый раз

### Файлы:
- `dataset_manager.py` - менеджер датасетов
- `server_with_datasets.py` - обновлённый сервер
- `MULTI_DATASET_GUIDE.md` - это руководство

### Следующий шаг:
Обновите веб-интерфейс с кнопками attach/detach!

---

**Happy training with multiple datasets! 📚🚀✨**
